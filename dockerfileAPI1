FROM nixos/nix

RUN nix-channel --update

RUN nix-env -i dotnet-sdk_3

# admin
ENV HOME=/root

RUN mkdir /data

WORKDIR $HOME

RUN dotnet publish -c Release

# CMD ["dotnet", "Uploader.Web.dll"]
CMD ["sleep", "infinity"]

EXPOSE 5000
EXPOSE 5001
